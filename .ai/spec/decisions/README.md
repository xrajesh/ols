# Architectural Decision Records

This directory holds lightweight decision records for the OLS workspace.

Each file follows the naming convention `NNNN-slug.md` (e.g., `0001-mcp-transport-choice.md`).
