# Spec Verification Summary — 2026-07-23

9 repos verified in parallel. Individual reports are in each repo's `.ai/spec/verification/spec-verify-2026-07-23.md`.

## Totals

| Repo | Constraint violations | Ref issues | Inconsistencies | Completeness gaps | Cross-repo issues |
|---|---|---|---|---|---|
| ols (main) | 2 | 5 | 3 | 4 | — |
| lightspeed-service | 0 | 5 | 4 | 3 | 2 |
| lightspeed-operator | 0 | 1 | 4 | 1 | 3 |
| lightspeed-console | 0 | 1 | 2 | 4 | 3 |
| lightspeed-agentic-operator | 0 | 3+1 | 3 | 4 | 3 |
| lightspeed-agentic-console | 0 | 2 | 3 | 4 | 2 |
| lightspeed-agentic-sandbox | 0 | 0 | 2 | 2 | 1 |
| lightspeed-otel-collector | 2 | 1 | 4 | 4 | 2 |
| lightspeed-rag-content | 0 | 2 | 1 | 1 | 0 |
| **Total** | **4** | **21** | **26** | **27** | **16** |

---

## Critical / High-Priority Findings

### Constraint Violations (4)

1. **ols/audit-logging.md** — Uses `[DEFERRED]` marker (×4); only `[PLANNED]` is defined in the convention.
2. **ols/rag-pipeline.md** — Uses `[PENDING]` marker; not a recognized convention.
3. **otel/collector.md** — Metrics silently dropped in routing mode, violating "MUST NOT drop silently" constraint.
4. **otel/pipeline.md** — mTLS required for cross-cluster transport by constraint 3, but no spec describes it.

### Broken References (selected high-impact)

5. **ols/system-overview.md** — References `lightspeed-hub/AGENTS.md` and `lightspeed-hub-ui/AGENTS.md`; both repos not in workspace.
6. **ols/repo-map.md** — 10+ spec file references for hub/hub-ui repos that don't exist.
7. **ols/repo-map.md** — `lightspeed-agentic-console/what/dynamic-components.md` does not exist.
8. **service/llm-providers.md** — Duplicate AWS Bedrock section (two conflicting `### AWS Bedrock` blocks with overlapping rule numbers 42-46).
9. **service/conversation-history.md** — Duplicate Rule 3 (numbering off by one from that point).

### Internal Inconsistencies (selected high-impact)

10. **service** — Tool approval field named `tools_approval.strategy` (tools.md) vs `tools_approval.approval_type` (security.md, system-overview.md). Same field, two names.
11. **operator/crd-api.md** — `toolBudgetRatio` default is `0.5` in one table, `0.25` in another (and in config-generation.md).
12. **operator/observability.md** — Lists only 4 condition types; 4 more (`AgenticConsolePluginReady`, `OtelCollectorReady`, `MCPServerReady`, `AlertsAdapterReady`) are missing.
13. **otel** — Schema creation responsibility contradicts: `postgres-exporter.md` rule 22 says it's the operator's job; `collector.md` and `postgres-exporter.md` rule 13 say `postgres_admin` extension creates it on startup.
14. **agentic-operator/audit-logging.md** — Finalizer connects directly to PostgreSQL; `templog.md` rules 14a-15 say it calls the Collector admin API. Cannot both be correct.
15. **agentic-operator/crd-api.md** — Duplicate rule 22 (two rules numbered 22).
16. **console/tools.md** — Wire format path is `tool_meta.olsUi.id` (confirmed by streaming.md); what/ spec calls it `olsToolUiID` in tool_meta.

### Stale PLANNED/DONE Markers

17. **agentic-operator** — `[PLANNED: OLS-3295]` in system-config.md and sandbox-execution.md; OLS-3295 is `[DONE]` in run-lifecycle.md and crd-api.md in the same repo.
18. **agentic-operator** — `[PLANNED: OLS-3018]` in run-lifecycle.md; EmergencyStopped is already fully specified.
19. **service/system-overview.md** — Marks Bedrock as `[PLANNED: OLS-1680]`; llm-providers.md shows it struck through as implemented.
20. **ols/audit-logging.md** — `[DEFERRED]` markers with no Jira tickets.
21. **ols/agentic-runs.md** — OLS-3295 (Proposal rename) still listed as planned; operators mark it done.

### Cross-Repo Alignment Failures

22. **operator → parent spec** — MCP server is standalone Deployment (operator spec); parent `deployment-lifecycle.md` still says "sidecar."
23. **operator → parent spec** — `deployment-lifecycle.md` missing `OtelCollectorReady` and `MCPServerReady` from the conditions list.
24. **operator → parent spec** — `deployment-lifecycle.md` still lists stale `--console-image-pf5`/`--console-image-4-19` flags.
25. **console → parent spec** — Parent `query-pipeline.md` says "conversation is added to the user's history sidebar"; console spec says no history sidebar exists (single session, no persistence).
26. **console → parent spec** — Parent spec says `reasoning` and `skill_selected` events are handled by console; neither appears in `chat.md` event table.
27. **agentic-operator → parent spec** — Per-run SA owner reference: operator spec prohibits (cross-namespace GC); parent `agentic-security.md` requires it.
28. **agentic-sandbox → parent spec** — Parent `agentic-runs.md` still says "Claude adapter"; sandbox spec says Claude was removed in OLS-3500, replaced by DeepAgents.

---

## Healthiest Repos

- **lightspeed-rag-content** — 2 ref issues, 1 inconsistency, no cross-repo problems. Clean parent alignment.
- **lightspeed-agentic-sandbox** — 0 broken references, well-cross-referenced, good internal consistency.

## Repos Needing Most Attention

- **lightspeed-service** — Duplicate Bedrock section is a structural defect; stale PLANNED markers; field name conflict.
- **lightspeed-otel-collector** — 2 constraint violations; hub/spoke planned features use MUST/SHOULD language without `[PLANNED]` markers throughout.
- **lightspeed-operator** — 4 inconsistencies; 3 parent spec alignment gaps (parent spec significantly stale vs operator reality).
- **ols (main)** — Hub/hub-ui repos not in workspace; `dynamic-components.md` broken reference; `[DEFERRED]`/`[PENDING]` non-standard markers.

---

## Recurring Theme: Parent Spec Lag

The parent OLS spec (`ols/.ai/spec/what/`) is consistently behind the child repo specs. Issues observed across multiple repos:
- `deployment-lifecycle.md` describes old MCP sidecar architecture (now standalone)
- `agentic-runs.md` uses old "Proposal" naming and "Claude adapter" terminology  
- `query-pipeline.md` has stale PLANNED markers for shipped features (Gemini, Anthropic, partially reasoning)
- Condition lists and flag inventories are incomplete vs what operators actually use

Recommended: a dedicated pass to bring the parent spec up to date with child repo realities.
